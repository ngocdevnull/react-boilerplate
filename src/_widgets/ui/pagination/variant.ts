import { cva } from 'class-variance-authority';

export const paginationVariants = cva('flex items-center justify-between gap-3');

export const paginationEllipsisVariants = cva('px-2 text-sm text-black-40');

export const paginationInfoVariants = cva('text-sm text-black-40');
